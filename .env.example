# Public browser-safe feature flags
NEXT_PUBLIC_ANALYTICS_ENABLED=false
NEXT_PUBLIC_ANALYTICS_DEBUG=false

# Optional deployment metadata may be supplied by the host or CI.
# VERCEL_GIT_COMMIT_SHA=
# GITHUB_SHA=
# COMMIT_SHA=

# Secrets must not be committed. Use local environment files or secure server-side storage.
